# config

Per-network deployment addresses, one JSON file per network (e.g. `sepolia.json`).
Written by the scripts in `/scripts` after deploying; read by the app.
Addresses only. Never put keys or RPC secrets here.
