# Copy to app/.env.local (Next.js) and fill in. Never commit real values.

# --- Server-only (never expose to the browser) ---
WORLD_RP_ID=
WORLD_SIGNING_KEY=
ATTESTER_PRIVATE_KEY=
RELAYER_PRIVATE_KEY=
SEPOLIA_RPC_URL=
GITHUB_TOKEN=
DATABASE_URL=

# --- Public (bundled into client code) ---
NEXT_PUBLIC_WORLD_APP_ID=
